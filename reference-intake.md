# 起草前对话澄清（详细）

## 先判轻重，再决定问多少

| 档位 | 怎么识别 | 问诊深度 |
| --- | --- | --- |
| **轻量** | 请示/函/一般通知/短报告；用户给的要点已够成篇；约 ≤1500 字 | 缺什么补什么（通常只要主送、事项、依据或金额）。**可不出**《任务确认单》，直接写 |
| **标准** | 汇报、方案、总结等中等篇幅 | 一轮关键问（对象层级、目的、要点、篇幅）→ 可出简版确认单或口头确认后写 |
| **加重** | 长汇报、讲话、调研、落实类部署；结构复杂或用户要求先提纲 | 按需多轮问诊 →《任务确认单》→ 确认后再写 |

另：**润色 / 改一段** → 确认修改重点即可，不走全套问诊。  
用户明确「先按现有信息写一版」→ 从简，标 `〔待补充〕`。  
**改 Word 原件** → 确认要批注 / 修订 / 两者兼有后，用 document-skill 写回 `.docx`（见 SKILL.md「改稿：优先在 Word 上出批注」）；不要默认只在对话里出《修改说明》。

**完整任务包**（齐则可少问）：文种 + 主送/对象层级 + 核心要点（事实清楚）+ 篇幅 +（可选）背景材料。

### 轻量示例：800 字请示

只需对齐：主送机关、请示事项、金额/权限依据、时限（如有）。  
结构：缘由 → 依据 → 请求事项 → `妥否，请批示。`  
不要：三阶段问诊、逻辑提纲、政治开篇长段、四遍润色、评分仪式。

## 问诊时必盯：对象级别

无论档位，**成稿名义与汇报/主送对象不清楚时先问清**（已写在需求里则不必再问）：

- 主送 / 汇报对象：哪一级、哪个单位或哪次会议？
- 成稿用哪个机关（部门）名义？
- 目的：批准、备案、通报、部署，还是争取支持？

站位随对象调，不默认「一律高站位」。短事务文少贴宏观表述。

## 标准 / 加重：可分阶段问（勿机械三轮）

每轮宜少问；用户一次给全则直接整理，不必凑满三轮。

### 阶段 A：任务定位

- 成稿名义；**对象层级与主送**
- 文种、场景、目的
- 是否落实类、须否政治开篇定锚（无原文标 `〔待核对〕`）
- 逻辑主线一句话（加重建议有；轻量可省）

### 阶段 B：输入收集

是否有背景材料（文件、批示、数据、往期稿、范文）？请 @ 或粘贴。  
PDF/DOCX/PPT/XLSX → 先 markitdown 转 `.md` 再读。不编造；缺处 `〔待补充〕`。

```bash
~/Projects/markitdown/.venv/bin/markitdown <输入文件> -o <输出.md>
```

### 阶段 C：输出规格

篇幅、重点、禁忌、是否先出提纲（仅加重或用户要求）。

## 《任务确认单》（标准/加重用；轻量可省略）

```text
【任务确认单】
成稿名义：
主送/汇报对象：（含级别）
文种/场景：
核心目的：
逻辑主线：（一句话；短稿可省略）
开篇定锚：是/否；依据或〔待核对〕
已有输入：
建议结构：
篇幅与形式：
重点突出：
禁用/敏感：
待您确认或补充：
```

## 文种专属问诊（按需取用，短稿勿全问）

### 请示（轻量优先）

1. 主送？一文一事是否单一请求？
2. 缘由与依据（政策、前次批复、紧急事由）？
3. 具体请求（金额、事项、时限）？

**结构**：缘由 → 依据 → 请求事项 → 请批语。

### 汇报材料

1. 场合与对象层级；通报还是要建议/协调？
2. 背景材料与核心事实？
3. 篇幅；是否落实类须定锚？

**结构（落实类）**：依据定锚 → 总体 → 做法 → 成效 → 问题 → 建议。  
非落实类、对象层级较低：可弱化定锚，直接写情况与事项。

### 讲话稿 / 发言稿

1. 会议性质、时长字数、听众层级  
2. 须体现的定点表述？落实类是否开篇定锚？  
3. 基调与是否收束到具体任务  

**结构**：称谓 →（定锚）→ 认识宜短 → 任务 → 落实 → 收束。

### 调研报告

1. 主题、授权、方式、样本与时间  
2. 建议给谁用；是否要「谁来做、何时完成」  

**结构**：方法 → 主要发现（可先列 N 条）→ 分章 → 研判 → 对策建议（与发现对应）。  
观察判断链见 [reference-logic.md](reference-logic.md) 范文 B。

### 工作方案 / 通知

方案：目标 → 任务 → 步骤 → 分工 → 保障。  
通知：依据 → 事项 → 要求 → 时限；对象责任写清即可。

## 润色/改稿输出格式

### 纯文本 / 聊天交付

```text
【正文】
（可交付稿）

【修改说明】
- 对象层级/站位：…
- 逻辑或结构：（如有）
- 敏感词：原表述 → 改写（如有）
- 仍待补充：…
```

轻量稿《修改说明》可极短，或仅列敏感词替换。

### Word 原件改稿（优先）

- 改动进文件：**修订痕迹**改字；**批注**写理由与建议（敏感词、逻辑、站位等）。
- 聊天仅短摘要（如「批注 12 处，修订主要在第三部分」）。
- 调用 document-skill 的 Document.`add_comment` / redlining，挂到对应段落或字句。
